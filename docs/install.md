# Установка

Требования, сборка из исходников, готовый установщик и портативная версия, удаление.

[← Обзор](../README.md) · [Все разделы руководства](README.md) · [🇬🇧 English](install.en.md)

## Что нужно

| Требование | Пояснение |
|---|---|
| Windows 10 или 11 (x64) | Windows 8.1 тоже подойдёт |
| .NET Framework 4.x | **уже есть в Windows**, ставить не надо |
| Права администратора | **не нужны для запуска.** Спрашиваются под конкретную операцию — очистка системных папок, Standby Memory, восстановление системных файлов |

Ни Visual Studio, ни .NET SDK, ни Node.js, ни какие-либо пакеты **не требуются** —
программа собирается компилятором `csc.exe`, который уже лежит внутри Windows.

Отдельно (по желанию, только для соответствующих вкладок):

- **winget** — для вкладки «Обновления». В Windows 11 и свежих Windows 10 он есть
  из коробки; если нет — установите «Установщик приложений» (App Installer) из
  Microsoft Store.
- **Chocolatey** — необязательный второй источник обновлений. Если он не установлен,
  вкладка просто работает через один winget.
- **Docker Desktop** — для вкладки Docker (нужен запущенный демон).

## Шаги

1. **Получите файлы.** Либо `git clone` репозитория, либо скачайте ZIP и распакуйте
   его в любую папку, например `C:\Tools\cleaner`.

2. **Если скачивали ZIP — снимите блокировку.** Windows помечает файлы из интернета, и
   сборка может отказаться работать. Правый клик по ZIP → **Свойства** → внизу галочка
   **«Разблокировать»** → OK, и только потом распаковывайте. Уже распаковали — выполните
   в PowerShell в папке проекта:

   ```powershell
   Get-ChildItem -Recurse | Unblock-File
   ```

3. **Запустите `run.bat`** двойным кликом. При первом запуске он соберёт
   `SysDeck.exe` и сразу откроет его. Дальше можно запускать сам `.exe`.

   Только сборка, без запуска:

   ```
   build.bat
   ```

4. **Запрос UAC при запуске не появится.** Программа работает с обычными правами
   пользователя. Windows спросит разрешение только тогда, когда вы запустите операцию,
   которой права действительно нужны, — и в вопросе будет видно, ради чего именно.

Всё. Устанавливать в систему ничего не нужно: `SysDeck.exe` —
самодостаточный файл, его можно просто носить с собой в папке.

## Готовые сборки: с установщиком и без

Команда `build-installer.bat` собирает обе сразу и кладёт их рядом, в папку `dist`:

| Что получается | Где |
|---|---|
| Установщик | `dist\SysDeck-Setup.exe` |
| Портативная версия | `dist\portable\` (старая папка удаляется и собирается заново) |
| Портативная версия одним архивом | `dist\SysDeck-portable.zip` |

Программа внутри собирается в `dist\build\` и в конце переносится, поэтому `SysDeck.exe` в корне
репозитория эта команда не трогает.

**Сборка при каждом `git push`.** Один раз выполните `tools\install-git-hooks.bat`: он кладёт в `.git\hooks`
обработчик `pre-push`. Дальше каждый `git push` собирает обе версии из отправляемого коммита (из чистой копии,
незакоммиченные правки в неё не попадают) и заменяет содержимое `dist\`. Отправку он не останавливает: если
сборка не удалась, об этом будет строка в выводе и журнал `dist\last-build.log`. Пропустить сборку один раз:
`SYSDECK_SKIP_BUILD=1 git push`.

**Релиз на GitHub.** После каждого push в `main` GitHub Actions (`.github/workflows/release.yml`) собирает то же
на машине с Windows и заменяет релиз `latest`: архив портативной версии и установщик. Постоянная ссылка на
архив — `https://github.com/rusyander/process-cleaner/releases/latest/download/SysDeck-portable.zip`.

Исходников в сборке нет, только готовый `.exe`. Но программы на .NET можно разобрать декомпилятором (ILSpy,
dnSpy) и увидеть код, близкий к исходному, — полностью закрыть его без обфускатора нельзя, а обфускатора в
составе Windows нет.

**Установка.** Запустите `SysDeck-Setup.exe`. По умолчанию программа
ставится только для вас, в `%LOCALAPPDATA%\Programs\SysDeck`, и никакого
запроса прав администратора при этом не появляется. Флажок «для всех пользователей»
переносит установку в `Program Files` — тогда Windows один раз спросит разрешение.
Можно выбрать другую папку и снять галочку с ярлыка на рабочем столе. Установщик создаёт
ярлык в меню «Пуск», запись в списке установленных программ и `uninstall.exe` рядом с
программой. Повторная установка поверх старой просто обновляет файлы: ярлыки не
задваиваются, настройки и история остаются на месте. Если программа в этот момент
запущена, установщик покажет это и предложит её закрыть — сам он ничего не завершает
без вашего согласия.

**Удаление.** «Параметры → Приложения → SysDeck → Удалить» или
`uninstall.exe` из папки программы. Будут удалены файлы, ярлыки, задача автозапуска,
запись в списке программ, ключи связи с браузерами и значение автозапуска загрузок. Флажок «удалить также настройки и историю» по умолчанию снят:
запомненный выбор, настройки и история лежат в `%APPDATA%\SysDeck` и
сохраняются до полного удаления программы — снятый флажок означает, что после новой
установки всё вернётся как было.

**Портативная версия.** Папка `portable` — та же программа без установки: распакуйте
куда угодно, хоть на флешку. Настройки, история и журналы пишутся рядом с exe, в подпапку
`Data`; за это отвечает файл `portable.marker` — держите его рядом с программой. Чтобы
удалить портативную версию, достаточно стереть папку: в системе не остаётся ничего.

## Переезд со старого имени

До 15.09.2026 программа называлась Windows Process Cleaner (`WindowsProcessCleaner.exe`). Первый запуск
`SysDeck.exe` переносит всё сам:

- останавливает фоновые процессы старой сборки («Захват», оверлей, «Размеры папок», загрузки) их же
  командой выхода, без принудительного завершения;
- переименовывает папку данных `%APPDATA%\WindowsProcessCleaner` в `%APPDATA%\SysDeck`. На старом месте
  остаётся точка соединения на новую папку: браузер продолжает находить распакованное расширение, а старое
  окно, если оно ещё открыто, пишет туда же. Если какой-то файл занят, программа работает со старой папкой
  и повторяет попытку при следующем запуске. Что перенесено, записано в `rebrand.log` в папке данных;
- переносит значения автозапуска в `HKCU\...\Run` и привязки `.torrent` / `magnet:` на новое имя и новый exe;
- при открытии окна спрашивает про задачи Планировщика (автозапуск, «Размеры папок», оверлей с правами) и
  правило брандмауэра для торрентов. После «Да» Windows один раз запрашивает права администратора, задачи
  создаются заново под именем SysDeck, старые удаляются.

Установщик SysDeck, запущенный поверх старой установки, убирает её ярлык, запись в списке программ и файлы.
Скачанную базу правил winapp2 нужно скачать заново: она лежала в `C:\ProgramData\WindowsProcessCleaner`.
Внутренние имена, от которых зависят уже установленное расширение и незавершённые загрузки, остались
прежними: хост `org.wpc.downloads` и суффикс недокачанных файлов `.wpcpart`.

## Если что-то не пошло

**«Windows защитила ваш компьютер» (SmartScreen).** Файл собран у вас локально и не
подписан сертификатом, поэтому Windows его не знает. Нажмите **«Подробнее»** →
**«Выполнить в любом случае»**.

**Антивирус ругается на свежесобранный `.exe`.** Это ложное срабатывание на
неподписанный файл. Добавьте папку проекта в исключения или соберите заново.

**`[ERROR] csc.exe of .NET Framework 4.x not found`.** В системе нет .NET Framework 4.x
(бывает на сильно урезанных сборках Windows). Установите .NET Framework 4.8 с сайта
Microsoft и повторите.

**`[ERROR] Build failed`.** Чаще всего программа уже запущена и держит свой `.exe`.
Закройте её (в том числе из трея — правый клик по иконке → «Выход») и соберите снова.

**Окно не появилось после запуска.** Вероятно, приложение уже работает: оно
однооконное и просто разворачивает уже открытое окно. Посмотрите в трей у часов.

**Не собирается из папки с очень длинным путём.** Перенесите проект ближе к корню
диска, например в `C:\Tools\cleaner`.

## Как удалить

1. В настройках снимите галочку **«Запускать вместе с Windows»** — задача
   `SysDeck` в Планировщике задач удалится.
2. Если включали связь с браузерами, снимите **«Связь с браузерами»** в настройках загрузок — ключи
   `NativeMessagingHosts\org.wpc.downloads` удалятся.
3. Закройте программу (трей → «Выход»).
4. Удалите папку проекта и папку настроек `%APPDATA%\SysDeck\`.

Служб программа не создаёт. В реестре остаётся только то, что включали вы: значения автозапуска в
`HKCU\...\Run` («Захват», незавершённые загрузки) и ключи связи с браузерами. Их убирают соответствующие
настройки.
